# Optional server-side token for higher GitHub API limits. Never expose it to the client.
GITHUB_TOKEN=

# Public development URL used for absolute links and metadata.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Required only when authenticated saved comparisons and history are connected.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
